import type { Accent } from "@/content/content";

export type { Accent };

export const bgClass: Record<Accent, string> = {
  paper: "bg-paper",
  clay: "bg-clay",
  teal: "bg-teal",
  ochre: "bg-ochre",
  sage: "bg-sage",
};
